# Overrides the production origin, without a trailing slash. Not needed in normal use: the
# domain (https://mandarinapt.me) is the default in astro.config.mjs, and Cloudflare Pages
# branch deployments use their own *.pages.dev address automatically.
# SITE_URL=https://mandarinapt.me
