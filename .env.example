# Production origin, without a trailing slash. Set it in the Cloudflare Pages build
# variables for the Production environment once the domain is connected.
# Enables canonical URLs, hreflang alternates, social preview URLs and the sitemap.
# Preview deployments use CF_PAGES_URL automatically when this is unset.
# SITE_URL=https://example.com
