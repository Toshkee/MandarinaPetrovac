# Optional production origin override, without a trailing slash.
# Vercel's production domain is used automatically when this is unset.
# Enables canonical URLs, hreflang alternates and sitemap generation.
# SITE_URL=https://example.com
